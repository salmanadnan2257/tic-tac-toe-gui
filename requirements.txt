# No third-party dependencies. Uses only the Python standard library
# (tkinter, included with the standard CPython installer on Windows/macOS;
# on Linux install python3-tk if it's missing). Requires Python 3.6+ for
# f-strings.
